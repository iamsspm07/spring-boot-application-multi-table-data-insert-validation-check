spring.application.name=genaicorecorp

# ============================
# Database Configuration
# ============================
spring.datasource.url=jdbc:postgresql://localhost:5432/genaicorecorp
spring.datasource.username=postgres
spring.datasource.password=Sujitmaity@143
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.connection-timeout=30000
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================
# Server Configuration
# ============================
server.port=8080

# ============================
# Logging Configuration
# ============================
logging.level.org.springframework=INFO
logging.level.com.yourpackage=DEBUG

# ============================
# Password Encoder Configuration
# ============================
# (Optional) Configure BCrypt strength
#spring.security.crypto.bcrypt.strength=12
